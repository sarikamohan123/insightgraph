# Core Web Framework
fastapi==0.128.0          # Modern async web framework
uvicorn==0.40.0           # ASGI server for FastAPI
pydantic==2.12.5          # Data validation
pydantic-settings==2.7.0  # Settings management from .env

# LLM Integration (NEW - Phase 1)
google-generativeai==0.8.3  # Gemini API client
python-dotenv==1.0.1        # Load environment variables from .env

# Database & Caching (for Phase 2 & 3)
redis==5.2.1              # Redis client for rate limiting
psycopg2-binary==2.9.10   # PostgreSQL adapter (sync)
sqlalchemy==2.0.36        # SQL ORM for database operations
alembic==1.14.0           # Database migrations
asyncpg==0.30.0           # Async PostgreSQL driver
greenlet==3.3.0           # Required for async SQLAlchemy

# Testing
pytest==8.3.4             # Testing framework (compatible with pytest-asyncio)
pytest-asyncio==0.24.0    # Async test support
pytest-mock==3.14.0       # Mocking utilities
pytest-cov==6.0.0         # Code coverage reports

# Development Tools
httpx==0.28.1             # HTTP client for testing APIs
ruff==0.14.11             # Fast Python linter & formatter (replaces Black, Flake8, isort)
